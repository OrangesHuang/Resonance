## 量化买卖规律发现方法论与可靠性评估

> 本文沉淀一次关于「买卖策略可靠性」的讨论：先诊断当前 ETF 共振系统买卖算法的可靠性，再厘清「拟合常量」与「独立发现的规律」的本质区别，最后给出一套可落地的规律发现流程。
> 结论先行：**当前系统"工程实现扎实、统计可靠性弱"——精心拟合了历史，但缺乏样本外验证。要把它从"描述过去"推向"预测未来"，核心是从「逐笔拟合」转向「对一类事件的总体统计」。**

---

### 一、当前买卖算法的可靠性诊断

#### 扎实的部分（可信）

1. **没有未来函数（最关键，通过）。** 所有指标都是滚动窗口、只用"过去 + 当日"数据：`calc_price_position` 用 `kline[target_idx-window+1 : target_idx+1]`（60 日窗口不含未来），`percentile_series`（成交额/融资分位）同理，策略内均线与持仓最高价也只累积历史。按日回放库里已存的 point-in-time 指标做回测，不存在数据泄漏，回测收益不是靠"偷看未来"得来的。

2. **确定性纯函数 + 分支单测。** analysis 层无 I/O，同样输入必同样输出，可复现；单测断言的是机制（每条买/卖路径是否触发），而非只盯收益数字。

3. **风控真实存在。** 硬止损 -5%、尾随止盈、卖出冷却、深背离"10 日未反弹即认错"的时间止损——是货真价实的防御机制，不是纯裸多。

#### 风险的部分（削弱可靠性）

1. **过拟合严重（最大问题）。** 几乎每个阈值都绑一个具体历史案例；旗舰策略约 40 个常量，个个标注日期。例如 `BEAR_BOTTOM_DIV_MIN=15.0` 的注释明写"假底 09-26 仅 -13.7 被拦"——阈值就是精确卡在已知赢家与输家之间。这是样本内曲线拟合。

2. **样本量太小，无统计显著性。** 沪深300 七年历史仅 6~8 轮完整买卖，所谓 "+440%" 由极少数几笔主导。一两笔运气好的交易就能撑起整个回测，没有置信区间、没有显著性。

3. **完全没有样本外验证。** 比对脚本只检查"Beta 没漏正式版买点 + 累计收益 ≥ 正式版"，没有留出期、没有 walk-forward、没有交叉验证——拟合与评估用的是同一段全历史。

4. **强 regime 依赖。** 策略硬分牛/熊，深度绑定 A 股宽基"2021-2024 熊 + 2025 牛"这一特定形态；"加速赶顶 vr>=3.5"这条路径就是照着 924 单一事件拟合的。

5. **回测↔实盘的两处偏差。** ①成交假设乐观：按信号当日收盘成交，而信号依赖当日份额，ETF 份额现实是 T+1 公布，实盘无法照此成交；②缺数据的不对称降级：多条路径要求份额/融资/成交额分位存在，回测有完整历史数据时会触发，实盘有缺口时静默不触发，造成回测与实盘行为不一致。

6. **复杂度带来的脆弱性。** 旗舰策略约 40 个相互作用参数 + 6 个状态标志，高度有状态、难推理，数据微小变化即可能翻结果。

---

### 二、"拟合常量" vs "独立发现的规律"

一个尖锐的反问：**参数最终不都是常量吗？凭什么说拟合的不是规律？**

答得对——**输出确实都是常量**。区别不在"是不是常量"，而在这个常量**怎么来的、以及它具备哪些性质**。

#### 重力加速度 g=9.8 也是常量

它是"独立发现的规律"，却照样是个写死的数字。它之所以算规律，是因为四个性质：

- **哪里都一样**：北京、纽约、巴黎扔东西都是 9.8。
- **稳**：取 9.79 还是 9.81，预测几乎不变（在"平台"上，不在"尖峰"上）。
- **能预测没见过的情况**：从没扔过的铁球照样算得准（样本外成立）。
- **背后有机制**：质量吸引，而非"调这个数让每次实验都对"。

反例：把你扔过的**每一个具体物体**各调一个专属 g，让每个恰好落在观测时刻——你也得到一堆常量，但它们是**拟合**，只能复述过去。

> **同样是常量，一个能预测未来，一个只能复述过去。差别是这个常量"测的是真效应"还是"描述的是巧合"。**

#### 落到本系统

`BEAR_BOTTOM_DIV_MIN = 15.0`（注释"假底 -13.7 被拦"）的性质是：

- **在尖峰上，不在平台上**：改成 14 或 16 就可能漏真底或多买假底，因为它照着 -13.7 这一个点卡出来。真效应的阈值应是"12~18 结果都差不多"。
- **没跨资产验证**：若"深背离见底"是真机制，沪深300/中证1000/科创50 应用大致同一阈值都work；可现实是每只 ETF 各一套专属 ~40 个数字，说明在拟合各自的特异性噪声。
- **自由度 >> 样本量**：40 个参数拟合 6 笔交易，数学上必然完美穿过且必然毫无意义。

#### 一个诚实的让步

这条线**不是二元的，是连续谱**。量化里没有纯粹的"发现"，所有工作都带拟合成分。市场还多一层反身性：就算真发现了规律，被套利后会衰减（不像 g=9.8 永远稳定）。所以更准确的提法不是"拟合 or 发现"，而是：**你有多少证据表明这个常量在测真东西，而不只是在描述这段特定的过去。** 当前系统的主要证据是"它在历史上把已知案例都对上了"——这是最弱的一种证据。

一句话：**常量的价值不在于它是常量，而在于你敢不敢拿它去赌一段它从没见过的数据。**

---

### 三、规律发现方法论（推荐流程）

核心只有一句：**先把"规律"当成可证伪的因果假设，再用事件研究（event study）去测它，而不是把 6 笔交易当成 6 个要去拟合的点。** 最大转变是从"逐笔拟合"换成"对一类事件的总体统计"。

#### 1. 先写假设，且在看任何收益之前写死

不是"我要抓 2020-03-23 那个底"，而是先写一句可证伪的机制陈述，例如：

> "ETF 份额单日大幅净赎回 + 价格急跌，反映的是**流动性冲击**（有人被迫卖）而非基本面恶化；被迫卖压释放后价格倾向均值回归。对手盘是抄底的做市/机构，所以效应会持续。"

关键是**同时写下证伪条件**："如果这类事件后 10 日收益分布不显著高于无条件基准，就推翻它。" 这一步强制"机制在前、数字在后"，堵死"事后给阈值补理由"。

#### 2. 把现象当分布来测，不当个案来 fit

解决"样本太小"的唯一出路。定义一个信号谓词（如"单日份额净赎回 > X 亿 + 当日跌幅 > Y%"），把全部 ETF、全部历史里满足它的**所有事件**捞出来——几十上百个，而不是 4 个底。对每个事件算**前向 5/10/20 日收益**，看这个**分布**（均值、中位数、胜率、离散度）。规律的定义变成"这一类事件的前向收益分布是否显著为正"。**6 笔交易 → 上百个观测**，自由度问题当场缓解。

#### 3. 一定要跟"零假设"比，算超额（edge）

"事件后平均涨 8%"本身没意义——任意一天买入也可能平均涨。要跟两个基线比：①无条件前向收益；②最笨的"逢跌就买"。**edge = 事件收益 − 基线**，再用 bootstrap 给置信区间。绝大多数"规律"会死在这一步——而这正是它的价值：**能杀掉的假设才值得留。**

#### 4. 参数是"带不确定度的测量值"，从平台区取

不取"让回测最高的值"，而是**画 edge 随参数变化的曲线**，从**平台区**挑（如 12~18 都差不多就取 15，并报告 CI）。曲线全是尖峰 = 噪声，放弃。参数越少越好、越"圆"越好——止损 5%（心理学依据）比"精确卡在 -13.7 之上的 15.0"可信得多。

#### 5. 跨资产普适才是主证据

若机制为真，**同一套参数应在所有 ETF 上都work**。拟合**一套共享参数**（池化所有资产），各标的偏离必须小且可解释（如按波动率缩放）。"一套参数打天下"本身，就是这条规律为真的最强证据。

#### 6. 留出期只看一次，事先写好放弃标准

最后 2 年设为 holdout，设计期间**绝不许碰**，最后看一次。放弃标准在跑之前就写好（如"样本外胜率掉到 55% 以下就砍"），防止事后合理化。

#### 7. 诚实面对两个约束

- **稀有事件拿不到显著性**：恐慌底一年可能一次。这时不假装回测"证明"了它，而是靠机制 + 跨市场类比支撑，并明确标注"证据等级低"。
- **反身性会衰减**：再真的规律被套利后也会退化。把"实盘 vs 回测偏离"当一等公民信号持续监控。

#### 一个关键取舍

**宁可规则更少、回测收益更低，也要更可能为真。** 当前 8 套专属策略 × 40 参数，建议压到 2~3 条机制支撑的池化规则，即使全历史收益从 +440% 掉到 +150%——因为后者更可能在没见过的数据上活着。此外，把**风控（止损/尾随）与 alpha（入场信号）分开**：尾随止盈本质是方差控制装置，不需每标的调参；把"发现"精力全集中在入场信号上。

---

### 四、可落地的下一步：事件研究脚手架

在 `scripts/` 下新增一个事件研究工具，复用现有 `compute_trades` 与指标计算：

- **输入**：一个信号谓词（份额/价格/量比/分位的组合条件）。
- **自动**：池化全 ETF、全历史满足该谓词的事件。
- **输出**：前向收益分布（5/10/20 日）、与无条件/逢跌就买基线的对比、参数平台曲线、bootstrap 置信区间。

它能把现有那些"规律"逐条拿去照妖——**一次性量化"这套策略到底是不是过拟合"**，是投入产出比最高的第一步。
